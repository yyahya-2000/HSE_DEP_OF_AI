import { createTheme } from '@mui/material'

export const globalBaseStyles = createTheme({
  palette: {
    primary: {
      main: '#2196f3'
    },
    secondary: {
      main: '#F44336'
    }
  }
})
